# KTX 감시 도구 인수인계

다른 Windows PC로 폴더만 옮겨도 동작하게 하기 위한 문서입니다.  
코레일톡에서 **KTX 333 (수서 → 동대구, 13:55~15:26)** 매진이 풀리면 예매하거나, 예약 대기면 신청 버튼을 누릅니다.

## 1. 이 프로젝트가 하는 일

| 파일 | 역할 |
| --- | --- |
| `android_control.py` | USB(ADB)로 폰 화면을 보고 클릭하는 GUI |
| `watch_ktx333.py` | 코레일톡 열차 목록을 새로고침하며 333호 매진 해제 감시 |
| `requirements.txt` | Python 패키지 목록 |
| `screenshots/` | 확인용 캡처. 없어도 실행됨 |

감시 순서:

1. 우측 상단 **새로고침** 반복
2. **KTX 333** 카드 오른쪽 **일반실 / 특실** 확인
   - 빨간 **매진** → 계속 새로고침
   - 초록 **예약 대기** → 그 칸 클릭 → **예약 대기 신청** → 특실 포함 체크 → `--phone` 안내 번호 입력 → 개인정보 동의 → 청색 **신청**
   - 매진/대기가 아니면 실제 좌석으로 보고 클릭
3. 실제 좌석인 경우 **바로 예매** → **장바구니 담기** 후 종료 (결제는 사람이 함)
4. 실제 좌석과 예약 대기가 같이 있으면 실제 좌석을 먼저 우선한다

## 2. 새 PC로 옮기기

### 2.1 복사할 것

프로젝트 폴더 전체를 복사합니다. 최소 필수 파일:

```
android_control.py
watch_ktx333.py
requirements.txt
인수인계.md
```

`screenshots/`, `__pycache__/` 는 복사하지 않아도 됩니다.

### 2.2 새 PC 소프트웨어

Windows 10/11, **Python 3.10 이상** (작성 시점 3.12.10).

PowerShell을 **관리자 아님**으로 열고 프로젝트 폴더에서:

```powershell
winget install Google.PlatformTools
py -3 -m pip install -r requirements.txt
```

`winget` 설치 후 터미널을 한 번 닫았다가 다시 엽니다. `adb`가 PATH에 없어도 스크립트가 아래 위치를 찾습니다.

- `adb` (PATH)
- `%ANDROID_HOME%\platform-tools\adb.exe`
- `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Google.PlatformTools*\platform-tools\adb.exe`
- `%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe`

연결 확인:

```powershell
adb version
adb devices -l
python android_control.py --list
```

`adb` 명령을 못 찾으면 전체 경로로 실행하거나, 터미널을 새로 연 뒤 다시 시도합니다.

삼성 폰이 안 잡히면 [삼성 USB 드라이버](https://developer.samsung.com/android-usb-driver)를 설치합니다.

### 2.3 폰 설정 (새 PC마다 다시 허용)

대상 기기 실측값: **Galaxy A15 (`SM-A155N`), 시리얼 `RF9X701HEZV`, 해상도 1080×2340, Android 16**.

1. 설정 → 휴대전화 정보 → 소프트웨어 정보 → **빌드번호 7번** → 개발자 옵션
2. 개발자 옵션에서 **USB 디버깅** 켜기
3. USB로 PC 연결
4. 폰에 **USB 디버깅을 허용하시겠습니까?** 가 뜨면 허용  
   새 PC는 RSA 지문이 다르므로 **반드시 다시 허용**해야 합니다.
5. `adb devices -l` 결과에 `device` 가 보여야 합니다. `unauthorized` 면 폰에서 허용을 안 한 상태입니다.

## 3. 실행 방법

폰 화면을 **코레일톡 → 수서→동대구 열차 목록**에 둔 채로 실행합니다.  
KTX 333 카드가 화면에 보여야 합니다.

```powershell
cd <이 폴더>
python watch_ktx333.py --check
python watch_ktx333.py --phone 01012345678
python watch_ktx333.py --dry-run --phone 01012345678
```

`--check` 가 성공하면 `screenshots/check_overlay.png` 에 파란 뱃지 박스와 빨간 매진 박스가 그려집니다.  
감시 중 중지: 터미널에서 `Ctrl+C`.

화면을 직접 보고 클릭하려면:

```powershell
python android_control.py
```

## 4. 무선 ADB (선택)

USB로 한 번 붙인 뒤:

```powershell
adb tcpip 5555
adb shell ip -o addr show wlan0
adb connect <폰IP>:5555
```

조건: PC와 폰이 **같은 Wi-Fi, AP 격리 없음**.

검토 결과 (`robotplus_guest`, PC `172.20.1.51` / 폰 `172.20.1.64`):

- 대역은 같음 (`172.20.1.0/24`)
- 폰은 `*:5555` 수신 중
- 게스트망 기기 격리 때문에 ping·TCP·`adb connect` 모두 실패

게스트망에서는 USB를 쓰십시오. 일반 Wi-Fi로 옮긴 뒤에만 무선을 다시 시도합니다.

## 5. 동작 원리 (문제 생길 때)

- 화면 캡처: `adb exec-out screencap -p`
- 터치: 유지된 `adb shell` 에 `input tap x y`
- KTX 333 식별: 목록 왼쪽의 짧은 파란 **KTX** 뱃지 (산천 뱃지보다 가로가 짧음, 폭 120px 미만)
- 매진 식별: 뱃지 기준 상대 좌표의 빨간 픽셀 수. 약 1100이면 매진, 400 미만이면 해제
- 새로고침 / 바로 예매 / 장바구니 담기: UI 덤프에서 버튼 글자·content-desc 로 탐색

실측 상수 (`watch_ktx333.py`, 1080×2340 기준):

```
일반실 매진 오프셋  y = 뱃지y + 79 ~ 118,  x = 961~1030
특실   매진 오프셋  y = 뱃지y + 158 ~ 197, x = 961~1030
새로고침 기본 좌표  (900, 149)
바로 예매 기본 좌표 (790, 2208)
예약 대기 신청 기본 좌표 (540, 2160)
```

매진은 빨간 픽셀, 예약 대기는 초록 '대기' 글씨로 구분합니다.

다른 기종·해상도·코레일톡 UI 변경 시 `--check` 로 박스를 확인하고 위 상수를 수정합니다.

## 6. 주의사항

- 감시 시작 전 폰이 **열차 목록 화면**이어야 합니다. 다른 화면이면 새로고침이 빗나갑니다.
- 매진 해제는 수 초 안에 다시 닫힐 수 있습니다. 결제·로그인·약관 화면은 자동으로 처리하지 않습니다.
- 코레일톡 캡처 제한 화면은 검게 보일 수 있습니다. 열차 목록은 캡처됩니다.
- `watch_ktx333.py` 와 `android_control.py` 를 동시에 켜도 ADB는 공유되지만, 감시 중 GUI로 다른 곳을 누르면 흐름이 깨집니다.
- 기기가 여러 대면 `--serial RF9X701HEZV` 로 지정합니다.

## 7. iOS

이 도구는 Android ADB 전용입니다. iPhone은 Windows에서 USB로 화면을 보고 클릭하는 동일 방식은 지원하지 않습니다.  
Mac + Xcode + Appium/WebDriverAgent 가 있어야 하고, 준비 비용이 큽니다. iOS가 필요하면 별도 설계가 필요합니다.

## 8. 빠른 점검 체크리스트

새 PC에서 아래가 되면 인수인계 완료입니다.

- [ ] `python --version` 이 3.10 이상
- [ ] `python android_control.py --list` 에 `device` 로 폰이 보임
- [ ] 코레일톡 열차 목록에서 `python watch_ktx333.py --check` 가 KTX 333 일반실·특실 매진을 출력
- [ ] `screenshots/check_overlay.png` 에 333호 오른쪽 매진 두 칸에 빨간 박스가 있음
- [ ] `python watch_ktx333.py` 로그에 `[n] 일반실=매진 ... 특실=매진` 이 반복됨
